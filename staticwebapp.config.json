{
    "routes": [
        {
            "route": "/api/*",
            "rewrite": "https://maxflow.app/api/wke-kepr-nzd"
        },
        {
            "route": "/*",
            "rewrite": "/index.html"
        }
    ]
}