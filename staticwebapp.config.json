{
    "routes": [
        {
            "route": "/api/*",
            "rewrite": "https://maxflow.app/api/wke-kepr-nzd"
        }
    ],
    "navigationFallback": {
        "rewrite": "/index.html"
    }
}