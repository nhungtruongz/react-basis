{
    "routes": [
        {
            "route": "/api/*",
            "methods": ["GET", "POST", "PUT", "DELETE"],
            "rewrite": "https://maxflow.app/api/wke-kepr-nzd"
        }
    ],
    "navigationFallback": {
        "rewrite": "/index.html"
    }
}